# planning/_archive/

Closed planning artifacts move here in batches (housekeeping Rule 2, `.context/housekeeping.md`): `YYYY-Qn/{stories,specs,plans}/<slug>.md`, keeping the slug. Dossiers never move — they stay in `planning/done-plans/`. Nothing here is loaded whole; grep the slug, then print the section.
