# src/

The root crate: `main.rs` (binary) or `lib.rs` (library). Modules as `src/<name>.rs` or `src/<name>/mod.rs`; one public concern per module. Anything two crates need moves to `crates/<name>/`.
