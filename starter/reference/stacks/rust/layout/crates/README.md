# crates/

Workspace member crates, one folder each (`crates/<name>/Cargo.toml`, `src/lib.rs`). Split a crate out when a second crate needs it or when its build time dominates; never for tidiness alone.
