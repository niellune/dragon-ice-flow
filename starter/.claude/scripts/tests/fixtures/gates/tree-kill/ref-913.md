# Gates — ref-913

## G1 — a child process finishes in time
CHECK: Start-Process (Get-Process -Id $PID).Path -ArgumentList @("-NoProfile","-Command","Start-Sleep 40; Write-Output tree-kill-marker-7f3a") -NoNewWindow -Wait; Write-Output late
EXPECT: ^late$
