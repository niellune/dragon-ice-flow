# Gates — ref-906

## G1 — command prints hello
CHECK: [string]::Join('', 'h','e','l','l','o')
EXPECT: ^hello$

## G2 — owner sees the export button in the browser
MANUAL: open /dashboard, the Export button is visible and downloads a CSV
