# Clean sample

The harness sets the project directory. Drift-guard tests ensure the pin holds; the merge is seamless at the boundary and robust to a rename. Navigate the wiki from its index. This is precision, not just polish. Crucially, every hit was checked.

A question with a long answer? The answer runs well past the forty-character cap the rule uses, so it is not a setup.

See https://example.com/delve and `delve` in code, ~~delve~~ struck, and the path a/delve/b.
