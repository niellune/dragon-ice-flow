# Wiki Log

> Chronological, append-only record of what happened in the wiki.
> Format: `## [YYYY-MM-DD] <operation> | <title>` followed by a short note.
> Operations: `ingest`, `query`, `lint`, `decision`, `feat`, `fix`, `refactor`, `docs`, `housekeep`.
>
> Quick scan with: `grep "^## \[" log.md | tail -10`
>
> The live file holds the current month only. Finished months move verbatim to `wiki/log/YYYY-MM.md` at housekeeping and are listed below. Never open an archive whole — grep the ruling, then print the section.

## Archived months

*(none yet)*

<!-- new entries go below this line -->
